# 项目精简化清理清单（2026-05-25）

**分支**: `feature/fix-tiktok-auth-routing`（staging / feature only）  
**禁止**: OAuth 核心、订单同步、analytics 口径、Nginx/PM2/deploy

## A. 移入 `backend/legacy-quarantine/`（原已删或未挂载，保留源码备查）

| 原路径 | 处置 | 引用检查 |
|--------|------|----------|
| `backend/routes/legacyDashboardRoutes.js` | → `backend/legacy-quarantine/routes/` | `registerApiRoutes` 未挂载 |
| `backend/routes/legacyRoutes.js` | → 同上 | 未 require |
| `backend/routes/deprecatedOpsAliases.js` | → 同上 | 由 `removedLegacyGone.js` 410 替代 |

## B. 移入 `legacy-quarantine/frontend/`（无 import）

| 原路径 | 处置 | 引用检查 |
|--------|------|----------|
| `frontend/src/components/dashboard/gmvCompareFetch.ts` | 隔离 | ripgrep 无 import |
| `frontend/src/components/dashboard/GmvCompareTrendPanel.tsx` | 隔离 | 仅 re-export，无 consumer |
| `frontend/scripts/extract-legacy.cjs` | 隔离 | 一次性抽取脚本，package.json 未引用 |

## C. 移入 `legacy-quarantine/docs/`（历史审计草稿，非运行依赖）

| 原路径 | 处置 |
|--------|------|
| `docs/audit-phase1-report.md` | 归档 |
| `docs/data-source-audit.md` | 与 `docs/audit/phase2-data-source-audit.md` 重复倾向 |

## D. 保留不动（仍有引用或主链路）

- `backend/tiktok-api/auth.js` / `authRouting.js` / `lib/tiktokOAuthUrl.js`
- `frontend/src/components/TikTokOAuthMarketButtons.tsx`
- `frontend/src/legacy/LegacyDashboardPage.tsx`（大屏）
- `backend/tiktok-api/ordersDashboardFromCache.js`（`dashboardShopGate`、`orderReconcileService`）
- `backend/scripts/*` 运维/门禁脚本（非 diagnose 已迁）

## E. 未找到（无需处理）

- `*.bak` / `*.backup` / `storage.local.bak` / `test-output` / 截图 — 工作区中不存在

## 回滚

```bash
git checkout HEAD -- <path>   # 单文件
git restore --source=HEAD~1 . # 整次提交前（慎用）
```
